{
  "name": "stale-invite",
  "version": "1.1.0",
  "description": "Invites users to a PR based on age",
  "main": "build/index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "dist": "jest && tsc && ncc build"
  },
  "keywords": [],
  "author": "mr-celo",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@vercel/ncc": "^0.36.0",
    "date-fns": "^2.29.3",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3"
  },
  "devDependencies": {
    "typescript": "^4.9.4"
  }
}
